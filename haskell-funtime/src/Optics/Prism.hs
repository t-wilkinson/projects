--{-# Language

-- #-}

-- | Play around with prisms

module Optics.Prism where

import           Control.Lens

{-
prism :: (Choice p, Applicative f) =>
  (b -> t) -> (s -> Either t a) -> p a (f b) -> p s (f t)



-}
